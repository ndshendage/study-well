names = Array.new(3);
p "names names = Array.new(3) = #{names.inspect}"

names1 = Array.new(3, "bharat");
p "names names = Array.new(3) = #{names1.inspect}"
names1 = Array.new(3);
nava = names.select{|element| p "#{element}"};
p "names names = Array.new(3) = #{nava.inspect}"
